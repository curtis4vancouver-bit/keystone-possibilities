# Deep Research: Circuit Breaker Patterns for Autonomous [[AGENTS|Agents]]: Deep research into circuit breaker algorithms for autonomous AI [[AGENTS|agents]]. How to detect infinite loops, no-progress states, and repeated error fingerprints in long-running agent tasks. Include the three diagnostic heuristics: No-Progress Trigger, Same-Error Fingerprint (hash-based), and Token Decline Metric. Compare implementations in LangGraph, ADK 2.0, and the-architect system. Include code examples for Python.
**Domain:** Agent Arch
**Researched:** 2026-06-10 09:07
**Source:** Google Deep Research via Chrome Automation

---

Script ran on page and returned:
```json
"Case Study: Keystone Sovereign Autonomous Circuit Breakers\n\nRunning autonomous workflows across construction logistics, the Shorts-to-Authority media pipeline, and the health content verticals requires hard, physical boundaries. When operating a localized, high-trust system out of Squamish—pushing data through a local RTX 5060 Ti (16GB VRAM) and 32GB DDR5 rig—compute is finite. If an agent hits a wall and spins, it doesn't just fail; it suffocates the machine. We need a rigid metabolic administration cadence for our compute cycles. If the system isn't moving dirt, we cut the power.\n\nHere is the exact technical blueprint for detecting and terminating dead-end agent states as of May 2026.\n\nI. The Three Diagnostic Heuristics\n1. No-Progress Trigger\n\nThis is the equivalent of running a compactor over the same square foot of grade. We track state mutations. If an agent executes an action but the resulting context object or workspace delta remains completely unchanged for consecutive ticks, it is stalled.\n\nMechanism: Hash the active state JSON. If the hash matches the previous n iterations, the execution loop is severed.\n\n2. Same-Error Fingerprint (Hash-based)\n\nWhen an agent attempts to hit the TooLost API or build a localized schema under builder license #52603, and fails, retrying the exact same payload wastes bio-efficiency cycles.\n\nMechanism: Generate a SHA-256 hash combining the tool_name, normalized_arguments, and the error_message. If that specific hash registers on the execution ledger three times in one session, the breaker trips.\n\n3. Token Decline Metric\n\nThis is the leading indicator of agent collapse. During long-horizon tasks, the context window degrades and the LLM's reasoning engine stalls. You will see a sharp drop in output tokens per step—going from 400 tokens of robust reasoning to single-word or empty outputs.\n\nMechanism: Track the derivative of output volume. If the generated token count falls below a minimum operational floor (e.g., < 50 tokens) for three consecutive steps, the model has entered a hallucination loop. Cut the circuit.\n\nII. Architectural Comparison (May 2026)\n\nLangGraph (v0.1.x): Highly structural. Circuit breakers are not native. You must manually wire conditional edges and state-inspection nodes after every single tool call. It works, but it bloats the routing logic because you are forced to build non-functional fault tolerance directly into the functional graph.\n\nGoogle ADK 2.0: Relies on event-driven telemetry. Breakers are deployed as interceptors on the MCP (Model Context Protocol) tool interface. It handles repetitive API failures well but struggles with the No-Progress state unless you configure aggressive cache-breaking and custom state introspection.\n\nthe-architect system: The most aligned with our grit. Its STEM architecture utilizes a Layer 4 Execution Engine with native, crosscutting breakers. It isolates fault tolerance from the reasoning engine entirely, deploying a hard \"Retry (2x) + circuit breaker\" alongside strict 30-second step timeouts. This keeps the core agent logic clean and prevents memory leaks.\n\nIII. Implementation: Keystone Sovereign Breaker Core\n\nHere is the Python implementation for the heuristics, designed to run lean on the local hardware constraints.\n\nPython\nimport hashlib\nfrom typing import Dict, List, Any\n\nclass AgentCircuitBreaker:\n    \"\"\"\n    Keystone Sovereign: Hardware-enforced agent circuit breaker.\n    Version: 1.2.0\n    \"\"\"\n    def __init__(self, max_stalls: int = 3, max_error_repeats: int = 2, token_floor: int = 50):\n        self.max_stalls = max_stalls\n        self.max_error_repeats = max_error_repeats\n        self.token_floor = token_floor\n        \n        self.state_hashes: List[str] = []\n        self.error_ledger: Dict[str, int] = {}\n        self.token_velocity: List[int] = []\n\n    def _hash_payload(self, payload: str) -> str:\n        \"\"\"Generates a stable hash for fingerprinting.\"\"\"\n        return hashlib.sha256(payload.encode('utf-8')).hexdigest()\n\n    def check_no_progress(self, current_state: str) -> bool:\n        \"\"\"Trips if the state remains identical for max_stalls.\"\"\"\n        state_hash = self._hash_payload(current_state)\n        self.state_hashes.append(state_hash)\n        \n        if len(self.state_hashes) >= self.max_stalls:\n            recent = self.state_hashes[-self.max_stalls:]\n            if len(set(recent)) == 1:\n                return True\n        return False\n\n    def check_error_fingerprint(self, tool: str, args: str, error: str) -> bool:\n        \"\"\"Trips if the exact same error + payload occurs repeatedly.\"\"\"\n        fingerprint = f\"{tool}|{args}|{error}\"\n        error_hash = self._hash_payload(fingerprint)\n        \n        self.error_ledger[error_hash] = self.error_ledger.get(error_hash, 0) + 1\n        return self.error_ledger[error_hash] >= self.max_error_repeats\n\n    def check_token_decline(self, tokens: int) -> bool:\n        \"\"\"Trips if the model's reasoning length collapses.\"\"\"\n        self.token_velocity.append(tokens)\n        if len(self.token_velocity) >= 3:\n            recent = self.token_velocity[-3:]\n            if all(t < self.token_floor for t in recent):\n                return True\n        return False\n\n    def evaluate_cycle(self, state: str, tokens: int, tool_data: Dict[str, Any] = None) -> str:\n        \"\"\"\n        Master evaluation gate. Run this after every agent tick.\n        \"\"\"\n        if self.check_no_progress(state):\n            return \"TRIP_NO_PROGRESS\"\n            \n        if tool_data and 'error' in tool_data:\n            if self.check_error_fingerprint(tool_data['name'], tool_data['args'], tool_data['error']):\n                return \"TRIP_SAME_ERROR\"\n                \n        if tokens > 0 and self.check_token_decline(tokens):\n            return \"TRIP_TOKEN_DECLINE\"\n            \n        return \"CLEAR\"\n"
```

---
*Auto-ingested into Keystone Brain Vector DB*


---
📁 **See also:** ← Directory Index

**Related:** [[20260613_AGENT_ARCH_security_patterns_for_autonomous_ai_agents_with_file_system_]] · [[20260613_AGENT_ARCH_advanced_gemini_api_patterns_for_production_ai_agents_in_mid]] · [[20260613_AGENT_ARCH_self-healing_error_recovery_patterns_for_autonomous_ai_agent]]

**Related:** [[20260613_AGENT_ARCH_multi-agent_coordination_patterns_for_autonomous_ai_systems_]]
