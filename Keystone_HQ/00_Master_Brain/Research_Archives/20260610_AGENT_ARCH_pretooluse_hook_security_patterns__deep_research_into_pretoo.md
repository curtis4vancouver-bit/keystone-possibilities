# Deep Research: PreToolUse Hook Security Patterns: Deep research into PreToolUse and PostToolUse lifecycle hooks for AI [[AGENTS|agents]] in 2026. How to implement synchronous tool-call interception that blocks dangerous commands, mutates tool arguments for safety (e.g., injecting --dry-run), and logs all tool executions for audit trails. Include implementation specs for Claude Code hooks, Antigravity SDK hooks, and ADK 2.0 hooks. Focus on patterns that prevent prompt injection attacks via tool arguments.
**Domain:** Agent Arch
**Researched:** 2026-06-10 09:05
**Source:** Google Deep Research via Chrome Automation

---

Script ran on page and returned:
```json
"Here is the blueprint for the Keystone Sovereign agent architecture. We are laying a deterministic control layer over the orchestration loop. In 2026, relying on system prompts for security is like trusting a handshake on a multi-million dollar structural build. You need hard physical limits.\n\nJust as a physical site requires rigid safety protocols before a machine is fired up, an AI agent requires synchronous interception before a tool executes. We use PreToolUse and PostToolUse lifecycle hooks to enforce strict operational boundaries, validate inputs, and neutralize prompt injections before they touch your local hardware or global legacy systems.  \nReddit\n\nThe Architecture: Interception and Control\n\nThe baseline execution loop of an agent is: Receive Input -> Call LLM -> Request Tool -> Execute Tool.\n\nHooks sever the direct line between the LLM and the execution environment. They act as synchronous checkpoints.\n\nPreToolUse (The Gate): Intercepts the raw JSON payload from the LLM before the tool fires. This is where we validate arguments, block destructive commands, and forcefully mutate parameters.  \nClaude Code by Anthropic | AI Coding Agent, Terminal, IDE\n+ 1\n\nPostToolUse (The Inspector): Captures the tool's output before it returns to the LLM. Used for sanitizing raw database dumps, formatting outputs for long-form stability, and feeding deterministic errors back to the model for self-correction.  \nDotzlaw Consulting\n\nNeutralizing Prompt Injection\n\nPrompt injection targeting tool arguments relies on tricking the LLM into embedding malicious payloads (e.g., rm -rf or SQL drops) into the tool's input parameters.\n\nTo neutralize this, validation logic must live in the orchestration layer, not the prompt. By passing all PreToolUse arguments through a deterministic sanitizer (like Google Cloud Model Armor or hard-coded regex validators) before execution, we strip out execution-level escapes.  \nMedium\n\nImplementation Spec: Claude Code Hooks\n\nClaude Code utilizes a JSON-over-stdin hook mechanism. It executes user-defined shell commands deterministically during its lifecycle. For Keystone Sovereign, this ensures local workstation operations on your hardware are locked down.  \nClaude Code by Anthropic | AI Coding Agent, Terminal, IDE\n+ 1\n\nConfiguration: Add the hook to your .claude/settings.json file.\n\nJSON\n{\n  \"hooks\": {\n    \"PreToolUse\": [\n      {\n        \"matcher\": \"\",\n        \"hooks\": [\n          {\n            \"type\": \"command\",\n            \"command\": \"bun run .claude/hooks/security_gate.ts\"\n          }\n        ]\n      }\n    ]\n  }\n}\n\n\nExecution Logic (TypeScript/Bun):\nThe hook reads the tool payload from stdin. You must output a JSON decision to stdout (allow, deny, or ask).\n\nTypeScript\n// security_gate.ts\nimport { readFileSync } from \"fs\";\n\nconst payload = JSON.parse(readFileSync(0, \"utf-8\"));\nconst toolName = payload.tool_name;\nconst args = payload.tool_input;\n\n// 1. Audit Trail Logging\nconsole.error(`[AUDIT] Tool: ${toolName} | Args: ${JSON.stringify(args)}`);\n\n// 2. Block Dangerous Commands\nif (toolName === \"Bash\") {\n    const cmd = args.command.toLowerCase();\n    const blocked = [\"rm -rf\", \"drop table\", \"> /dev/sda\"];\n    \n    if (blocked.some(pattern => cmd.includes(pattern))) {\n        console.log(JSON.stringify({\n            decision: \"deny\",\n            reason: \"Core system protection active. Destructive command blocked.\"\n        }));\n        process.exit(0);\n    }\n}\n\n// 3. Mutate Arguments (Enforce constraints)\nif (toolName === \"DeployContent\") {\n    // Deny and instruct the model to append the required flag.\n    if (!args.flags || !args.flags.includes(\"--dry-run\")) {\n        console.log(JSON.stringify({\n            decision: \"deny\",\n            reason: \"Missing --dry-run flag. All unverified operations must use --dry-run.\"\n        }));\n        process.exit(0);\n    }\n}\n\nconsole.log(JSON.stringify({ decision: \"allow\" }));\nprocess.exit(0);\n\nImplementation Spec: Google Antigravity SDK Hooks\n\nThe Antigravity SDK (Python) is utilized for robust applications and integrates deeply with secondary nodes. Antigravity classifies hooks into three tiers: Inspect (logging), Decide (blocking), and Transform (mutating).  \nGoogle Antigravity Documentation\n+ 1\n\nExecution Logic (Python):\nWe use a Transform hook to forcefully inject parameters, ensuring the agent aligns with the required operational cadence.\n\nPython\nfrom google.antigravity import Agent, LocalAgentConfig\nfrom google.antigravity.hooks import TransformHook, DecideHook\n\nclass AuditAndBlockHook(DecideHook):\n    async def on_pre_tool_use(self, context, tool_call):\n        # 1. Audit Trail\n        print(f\"[AUDIT] {tool_call.name} executing with {tool_call.args}\")\n        \n        # 2. Block Logic\n        if tool_call.name == \"execute_sql\" and \"DROP\" in str(tool_call.args).upper():\n            return False, \"SQL DROP operations are permanently disabled.\"\n        return True, \"\"\n\nclass MutateArgumentHook(TransformHook):\n    async def on_pre_tool_use(self, context, tool_call):\n        # 3. Argument Mutation: Inject --dry-run\n        if tool_call.name == \"publish_intel_briefing\":\n            if \"flags\" not in tool_call.args:\n                tool_call.args[\"flags\"] = []\n            if \"--dry-run\" not in tool_call.args[\"flags\"]:\n                tool_call.args[\"flags\"].append(\"--dry-run\")\n        return tool_call\n\nconfig = LocalAgentConfig(\n    system_instructions=\"You are the orchestrator.\",\n    hooks=[AuditAndBlockHook(), MutateArgumentHook()]\n)\n\nImplementation Spec: Google ADK 2.0 Hooks\n\nADK 2.0 focuses on stateful multi-agent orchestration. It utilizes before_tool_callback and after_tool_callback within its graph-based engine. This is ideal for managing the handoffs between different domain agents.  \nGoogle Blog\n\nADK 2.0 uses session state integration, allowing the hook to verify the context of the operation against established bio-efficiency cycles or current project phases.\n\nExecution Logic (Python):\n\nPython\nfrom google.adk.tools import Tool, ToolContext\nfrom typing import Any, Dict, Optional\nimport logging\n\nlogging.basicConfig(filename='keystone_audit.log', level=logging.INFO)\n\ndef before_tool_callback(\n    tool: Tool, \n    input_args: Dict[str, Any], \n    callback_context: Any\n) -> Optional[Dict]:\n    \n    # 1. Audit Logging\n    logging.info(f\"Execution requested: {tool.name} | Payload: {input_args}\")\n\n    # 2. State-Aware Blocking\n    current_phase = callback_context.state.get(\"project_phase\")\n    \n    if tool.name == \"push_to_production\":\n        if current_phase != \"Phase 3\":\n            # Short-circuit the tool. The LLM receives this error directly.\n            return {\n                \"error\": \"Access denied\",\n                \"reason\": f\"Production pushes are locked during {current_phase}.\"\n            }\n\n    # 3. Argument Validation\n    if tool.name == \"social_seo_distribute\":\n        target = input_args.get(\"platform\")\n        if target not in [\"TikTok\", \"YouTube\", \"Spotify Canvas\"]:\n             return {\n                \"error\": \"Invalid target\",\n                \"reason\": \"Platform not recognized in distribution schema.\"\n            }\n\n    # Return None to let the framework execute the tool\n    return None \n\nThe Bottom Line\n\nTreat the LLM as an unreliable subcontractor. It generates the intent, but the hooks execute the contract. By hardcoding your constraints into the PreToolUse layer, you guarantee the integrity of your infrastructure, whether you are running localized schema from your primary base or pushing heavy content down the global pipeline."
```

---
*Auto-ingested into Keystone Brain Vector DB*


---
📁 **See also:** ← Directory Index

**Related:** [[20260613_AGENT_ARCH_security_patterns_for_autonomous_ai_agents_with_file_system_]] · [[20260613_AGENT_ARCH_advanced_gemini_api_patterns_for_production_ai_agents_in_mid]] · [[20260613_AGENT_ARCH_self-healing_error_recovery_patterns_for_autonomous_ai_agent]]

**Related:** [[20260610_AGENT_ARCH_circuit_breaker_patterns_for_autonomous_agents__deep_researc]]
